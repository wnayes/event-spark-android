<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- Retrieve directions to event. -->
    <item android:id="@+id/menu_get_directions"
          android:icon="@drawable/location_directions"
          android:title="@string/menu_get_directions"
          android:showAsAction="ifRoom"/>

    <!-- Refresh event information. -->
    <item android:id="@+id/menu_refresh_event"
          android:icon="@drawable/refresh"
          android:title="@string/refresh"
          android:showAsAction="ifRoom"/>

</menu>

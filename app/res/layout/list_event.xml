<!-- Layout of list items containing events. -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <!-- This shows the event type color to the left of each list item. -->
    <View android:id="@+id/list_event_colorbar"
        android:layout_width="7dp"
        android:layout_height="fill_parent"
        android:background="#ea9d38"/>

	<LinearLayout
	    android:layout_width="fill_parent"
	    android:layout_height="wrap_content"
	    android:orientation="vertical"
	    android:paddingTop="5dip"
	    android:paddingBottom="5dip"
	    android:paddingLeft="10dip">
	    <TextView
	        android:id="@+id/list_event_title"
	        android:layout_width="fill_parent"
	        android:layout_height="wrap_content"
	        android:ellipsize="end"
	        android:maxLines="1"
	        android:textSize="18sp" />
	    <TextView
	        android:id="@+id/list_event_description"
	        android:layout_width="fill_parent"
	        android:layout_height="wrap_content"
	        android:ellipsize="end"
            android:maxLines="1"
	        android:textSize="12sp" />
    </LinearLayout>

</LinearLayout>
